let log = function(message) {
    console.log(message);
}

let doLog = (message) => {
    console.log(message);
}

// Basically a lambda function
let oneLine = (message) => console.log(message);

let shortest = message => console.log(message);
